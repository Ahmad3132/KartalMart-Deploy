{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "npm run build && node --loader tsx server.ts",
    "healthcheckPath": "/api/ping",
    "restartPolicyType": "ON_FAILURE"
  }
}
